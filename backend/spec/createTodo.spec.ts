const event = require('./data/event.json')

let userId
describe('Create Todo', () => {
    it('should have a todo payload', () => {
         expect(1).toBe(2)
        // expect(getUserId).toHaveBeenCalledTimes(1)
    })
    it('should have a user id', () => {
    })
    it('should create a todo', () => {
    })
})